name: run-something

on:
  push

permissions:
  contents: read

jobs:
  test-run-thing:
    name: Doesn't do anything
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: conda-incubator/setup-miniconda@v2
        with:
          channels: conda-forge,kwelsh-lanl,defaults
          activate-environment: fierro-env
          auto-activate-base: false
          use-only-tar-bz2: true
      
      - name: check solution
        run: |
          conda create -y --name fierro fierro-cpu

      - name: test fierro
        run: |
          which fierro
          fierro --help